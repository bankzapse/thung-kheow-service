/**
 * ธงเปิด/ปิดฟีเจอร์ (production toggles)
 *
 * PICKUP_ENABLED = ระบบรับซื้อของเก่าหน้าบ้าน (เรียกรถมารับ + ออกบิล)
 * โปรดักชันเฟสแรกเน้น "Drop & Go" อย่างเดียว → ปิดไว้ก่อน (โค้ดยังอยู่ครบ เปิดได้ทีหลัง)
 */
export const PICKUP_ENABLED = false;
